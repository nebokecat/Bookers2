        <% if post_book.errors.any? %>
            <div id="error_explanation">
              <h2><%= pluralize(post_book.errors.count, "error") %> prohibited this book from being saved:</h2>
        
              <ul>
                <% post_book.errors.full_messages.each do |message| %>
                  <li><%= message %></li>
                <% end %>
              </ul>
            </div>
        <% end %>
<div class="col-xs-3">
    <h2>User info</h2>
    <%= attachment_image_tag user, :profile_image,size: "100x100", format: 'jpeg', class: "pull-left profile-img", fallback: "no_image.jpg" %>
    <table class="table">
        <tr></tr>
        <tr>
            <td>name</td>
            <td><%= user.name %></td>
        </tr>
        <tr>
            <td>introduction</td>
            <td><%= user.introduction %></td>
        </tr>
    </table>
    <div class="row">
        <%= link_to "",edit_user_path(user.id),class:"col-xs-12 btn btn-default glyphicon glyphicon-wrench" %>
    </div>
    <h2>New book</h2>
     <%= form_for(post_book, url: books_path) do |f| %>
        <div class="row field">
            <label>Title</label>
            <%= f.text_field :title,class:"col-xs-12" %><br>
        </div>
        <div class="row field">
            <label>Opinion</label>
            <%= f.text_area :body,class:"col-xs-12" %>
        </div>
        <div class="row">
            <%= f.submit "Create Book", class:"btn btn-primary col-xs-12" %>
        </div>
    <% end %>
</div>